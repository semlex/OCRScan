import { makeStyles } from '@mui/styles'

export const useHomeStyles = makeStyles(() => ({
  listItem: {
    color: '#656363',
    textDecoration: 'none',
    backgroundColor: '#dfe5ff',
    borderRadius: 1.5,
  },
}))
